# Backend target for the same-origin /api proxy (backend alias in production)
API_PROXY_TARGET="http://localhost:4000"

# One-click demo login (baked at build time)
NEXT_PUBLIC_DEMO_EMAIL="demo@verba.app"
NEXT_PUBLIC_DEMO_PASSWORD="demo1234"
